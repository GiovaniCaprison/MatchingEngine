# Maven build output
target/

# CMake build output, including the fetched generator and test framework
cpp/build/

# clangd's index, rebuilt by the editor from the compile database
cpp/.cache/

# Measurement runs, which are pulled off the machine that made them rather than committed
results/

# Eclipse / m2e / jdtls generated project metadata
.classpath
.project
.settings/
.factorypath

# OS / editor cruft
.DS_Store
