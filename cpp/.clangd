# Points clangd at the compile database the configure step writes, so the editor compiles each
# file with the flags the build uses. Run cmake -B build once after a fresh clone or the editor
# has nothing to read.
CompileFlags:
  CompilationDatabase: build
