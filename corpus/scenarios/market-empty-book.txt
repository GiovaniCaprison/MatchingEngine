# A market order never rests, so on an empty book it is accepted and then removed in full.

INSTRUMENT tick=5 lot=1 scale=4 min=1 max=1000000 band=500 open=100000 alloc=PRICE_TIME

SESSION    CONTINUOUS
STATE      CONTINUOUS

NEW        SELL MARKET IOC - 50
ACCEPTED   #1
REMOVED    #1 50 IMMEDIATE_OR_CANCEL_REMAINDER
